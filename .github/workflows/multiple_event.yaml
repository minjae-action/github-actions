name: multiple-event-worklfow
on:
  push:
  issues:
    types:  [opened]
  workflow_dispatch:

jobs:
  multiple-event-job: 
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step 2
        run: |
          echo hello world
          echo gtihub action
    
